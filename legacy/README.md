# Legacy

`advertising.py` contains the original prefix-based event advertising commands from the early version of Patzer Bot (pre-2021). These were later dropped when the bot was rebuilt as a chess-only tool using Discord's slash command API.

Kept here for historical reference only — not part of the active bot.
